const { sayHi, hello } = require("./hello");
// const readFilePromise = require("../transform-callback-promise/index");

// readFilePromise("fileOne.txt", { encoding: "utf-8" }).then((data) => {
//   console.log("result", data);
// });

sayHi();
hello();
